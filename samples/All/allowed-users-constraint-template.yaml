apiVersion: opa.k8smgmt.io/v3
kind: OPAConstraintTemplate
metadata:
  labels:
    rafay.dev/opa: template
  name: allowed-users-custom
  project: defaultproject
spec:
  artifact:
    artifact:
      paths:
      - name: file://ct/allowed-users.yaml
    options: {}
    type: Yaml

